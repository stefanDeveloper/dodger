# OpenLDAP

To run docker-compose up you have to adapt `.env`

## Usage

Set the following environment variables:

```sh
PASSWORD=
DOMAIN=
DASHBOARD_PASSWORD=
NAME=
BASE_DN=
```

And then run

```sh
docker-compose up -d
```
